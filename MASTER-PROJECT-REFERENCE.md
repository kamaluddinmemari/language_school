# فایل جامع شناسایی و بازیابی پروژه — سیستم مدیریت آموزشگاه زبان

این فایل کامل‌ترین مرجع پروژه‌ست؛ هدفش اینه که هر Claude (یا هر توسعه‌دهنده‌ی دیگه‌ای)
با خوندن همین یه فایل، کل وضعیت پروژه رو از صفر تا الان بفهمه و بتونه دقیقاً از
همین‌جا ادامه بده. این رو همیشه به‌روز نگه‌دار و کنار بقیه‌ی فایل‌های یادآور
(PROJECT-FULL-CONTEXT.md، فایل پوش‌نوتیفیکیشن، فایل Deploy، فایل Network Error)
توی گیت‌هاب پوش کن.

---

## ۱. معرفی کلی پروژه

سیستم مدیریت یک آموزشگاه خصوصی زبان، متشکل از سه بخش به‌هم‌وصل:
- **بک‌اند:** Django REST Framework (ریپو: `language_school`)
- **پنل ادمین:** React (Create React App) — برای مدیر، استاد، و کارمندان اداری
- **اپ موبایل:** Expo / React Native (SDK 56) — برای استاد و دانش‌آموز

همه‌ی زبان رابط کاربری (UI)، برچسب فیلدها، و پیام‌های خطا **فارسی** هستن.

سه نقش اصلی کاربری: **ادمین**، **استاد** (و نقش‌های شبیه‌استاد: ارزیاب/مدیر آموزش،
office/کارمند اداری)، **دانش‌آموز**.

سه فاز کاری پروژه (از ابتدا):
- **فاز ۱:** کلاس‌های خصوصی، با گردش‌کار ارجاع دو مرحله‌ای (ادمین → استاد → تکمیل)
- **فاز ۲:** کلاس‌های گروهی/ورکشاپ
- **فاز ۳:** تعیین سطح، با پنل جدای ارزیاب

---

## ۲. تاریخچه‌ی کامل توسعه (خلاصه‌ی همه‌ی دورها)

### پایه‌گذاری اولیه
- رفع باگ اولیه‌ی متد `save()` (فراموش‌شدن `**kwargs`)
- گردش‌کار ارجاع دو مرحله‌ای استاد
- ساخت صفحات موبایل از صفر (ثبت‌نام دانش‌آموز، درخواست کلاس با آپلود عکس رسید،
  دکمه‌های اقدام استاد، امتیازدهی رضایت)
- اصل معماری: هر فاز یه اپ جنگوی مستقل، با `notifications/utils.py` مشترک برای
  نوتیف‌های داخلی
- ساخت الگوی `PROJECT-FULL-CONTEXT.md` برای بازیابی کامل پروژه از گیت‌هاب

### دورهای ۱۹ تا ۴۱ (میان‌مدت)
- نقش ارزیاب (`مدیر آموزش`) با `User.TEACHER_LIKE_ROLES`
- حضور و غیاب چندجلسه‌ای برای کلاس‌های گروهی
- قیمت‌گذاری هر جلسه با سهم قابل‌تنظیم استاد/آموزشگاه
- `GroupPriceSetting` (تک‌نمونه‌ای/Singleton) با override هر ورکشاپ
- کامپوننت تاریخ جلالی (Jalali) به‌جای همه‌ی تقویم‌های میلادی — پیاده‌سازی خالص
  جاوااسکریپت، بدون پکیج خارجی
- شناسایی الگوی نصب اشتباه: کاربر گاهی فایل‌ها رو تکی کپی می‌کرد نه کل پوشه‌ی
  `src/` — راه‌حل: همیشه صریح دستور «اول پوشه‌ی کد رو پاک کن، بعد زیپ جدید رو باز کن»
- از دور ۲۷ به بعد: build پنل ادمین با `CI=true npx react-scripts build` تست
  می‌شه تا خطاهای واقعی کامپایل دیده بشن
- بعد از هر deploy تازه: همیشه `python manage.py migrate` و
  `python manage.py reset_admin` لازمه

### دورهای ۴۲ تا ۵۲
- مدیریت کلاس مازاد (surplus) با منطق توزیع خودکار
- ماژول کامل حقوق و دستمزد (پروفایل کارمند، پروفایل حقوق، حقوق ماهانه با محاسبه‌ی
  بیمه، اضافه‌کاری/غیبت/پاداش، تأیید فیش حقوقی توسط کارمند)
- سیستم مرخصی با سهمیه‌ی ساعتی ماهانه و سه دسته‌ی مرخصی
- تخصیص خودکار کلاس با ترجیحات اختیاری استاد/روز/زمان هر سطح
- گردش‌کار مجوز ورود/خروج با ارجاع ادمین‌به‌استاد و ثبت خودکار «دیده‌شدن»
- نقش جدید `office` (کارمند اداری) با دسترسی گسترده به‌جز محدودیت دید حقوق/مرخصی
- ادغام تعیین سطح با ثبت‌نام مستقیم دانش‌آموز
- چاپ در همه‌ی ۱۲ صفحه با روش HTML-to-.xls بدون وابستگی خارجی
- رنگ‌بندی کتابخانه بر اساس نوع مجموعه

**باگ‌های مهم رفع‌شده در این بازه:**
- شکست deploy به‌خاطر پکیج npm جدید (`xlsx`) — قانون ثابت شد: کاربر هرگز
  `package.json` رو جایگزین نمی‌کنه، فقط `src/`؛ پس هیچ پکیج npm جدیدی بدون
  اطلاع‌رسانی صریح اضافه نمی‌شه
- باگ `JalaliDateInput`: فیلدهای تاریخ اجباری ظاهراً پرشده نشون داده می‌شدن ولی
  مقدار خالی می‌فرستادن
- از دست‌رفتن داده‌ی دانش‌آموز مازاد حین تأیید نهایی کلاس
- فیلد FK گم‌شده‌ی `student` در `LevelTestIntakeSerializer`

### دور ۵۷ تا ۶۰ — ثبت‌نام مستقیم دانش‌آموز (وب+اپ) + پیشنهاد هوشمند سطح + گردش‌کار تایید
- ویژگی بزرگ «ثبت‌نام مستقیم دانش‌آموز»: هم پنل وب («🆕 ثبت‌نام مستقیم دانش‌آموز»)
  هم اپ («ثبت‌نام دوره ترمیک») — سطح پیشنهادی خودکار، کلاس‌های مجاز، پرداخت
  (وب: همه‌ی روش‌ها؛ اپ: فقط کارت‌به‌کارت با آپلود رسید اجباری یا درگاه/به‌زودی)
- تابع مرکزی `_compute_level_suggestion`: اعتبار سطح ۶۰ روزه + تشخیص سطح پایانی
  گروه سنی (i5/teen15) + مدل تازه‌ی `LevelRenewalApproval` (گردش‌کار تایید مدیر
  آموزش به‌جای تعیین‌سطح مجدد) — پنل «🎓 تایید تمدید سطح»
- ثبت‌نام‌های کارت‌به‌کارتِ خودِ دانش‌آموز تا تایید مدیر «در انتظار»ند و اصلاً وارد
  لیست کلاسی نمی‌شن؛ مودال مرکزی که هر بار وارد صفحه‌ی مدیریت کلاس‌ها می‌شی
  خودکار باز می‌شه اگه چیزی در انتظار باشه
- یکتایی کد ملی بین کلاس‌ها (یه ثبت‌نام فعال هم‌زمان)
- سوابق آموزشی هر دانش‌آموز (سطح فعلیِ همیشه‌به‌روز + تاریخچه‌ی ترمیک)
- سطوح سفارشی در تعریف شهریه (دکمه‌ی +، برای سطوح خارج از الگوی استاندارد)
- فلوتینگ‌شدن نوار بالای صفحات، مخفی‌سازی پیش‌فرض آمار/تخصیص، فیلد تاریخ جلسه
  زیر هر کلاس، نمایش کم‌رنگ مبلغ پیش‌فرض شهریه
- اتصال نهاییِ تعیین‌سطح به لیست دانش‌آموزان: وقتی تعیین‌سطح تکمیل می‌شه، خودکار
  یا به حساب دانش‌آموزیِ موجود (بر اساس کد ملی) وصل می‌شه یا (با فیلد تازه‌ی
  `gender`) یک حساب تازه ساخته می‌شه
- ⚠️ جزئیات کامل فنی این چهار دور توی `ROUND-57-60-DIRECT-ENROLLMENT-WORKFLOW.md` هست

### دور ۵۵ و ۵۶ — رفع باگ ریشه‌ای «تخصیص خودکار vs ثبت‌نام واقعی» + تفکیک کلاس + سرچ کتابخانه
- current_count (عدد انتزاعیِ تخصیص خودکار) و ثبت‌نام واقعی کاملاً از هم جدا شدن؛
  رنگ/وضعیت کارت کلاس از `real_enrolled_count` (محاسبه‌ی زنده) میاد
- تفکیک کلاس (Split): تصادفی یا دستی، خودکار کلاس هم‌سطح خالی پیدا/می‌سازه
- الگوریتم تخصیص خودکار: اولویت با کلاس‌های بزرگ‌تر و کم‌شماره‌تر
- گروه سنی از روی خودِ کد سطح تشخیص داده می‌شه (نه آزمون تعیین‌سطح دانش‌آموز)
- سوابق مالی دانش‌آموز + گزارش دقیق ثبت‌نام (فیلتر سطح/روز/جنسیت/بازه‌ی شمسی)
- `print-table.js`: پرینت A4 افقی با فونت خودکار؛ سرچ اساتید؛ سرچ فلوتینگ کتابخانه
- ⚠️ جزئیات کامل فنی توی `ROUND-55-56-CLASS-MANAGEMENT-BUGFIXES.md` هست

### دور ۵۴ — شهریه/تخفیف/استرداد/کیف‌پول در مدیریت کلاس‌ها + بهبودهای دانش‌آموزان
- `TuitionSetting` (شهریه‌ی مصوب سطح×گروه‌سنی)، `DiscountedPerson` (افراد دارای
  تخفیف، خودکار از ثبت‌نام‌های تخفیف‌دار)، `EnrollmentRefund` (استرداد با
  شماره‌کارت/نام گیرنده/تاریخ خودکار)، `WalletTransaction` + `wallet_balance`
  روی `User` (کیف پول دانش‌آموز)
- سه دکمه‌ی جدید روی هر دانش‌آموزِ ثبت‌نام‌شده در `ClassManagement.js`: استرداد،
  انتقال به کلاس هم‌سطح/هم‌جنسیت دیگر، انتقال شهریه به کیف پول
- بازطراحی نمایش لیست کلاس‌ها: ستون‌بندی بر اساس ساعت دقیق (خط جداکننده‌ی
  عمودی + برچسب صبح/عصر)، فونت درشت سطح/عنوان، فیلتر ساعتی + جستجوی
  سطح/استاد/جنسیت، مخفی‌سازی پیش‌فرض «لیست کلاس‌ها با استاد هرکدام»
- `Students.js`: پیکر تاریخ تولد شمسی (پیاده‌سازی مستقل، نه از طریق کامپوننت
  مشترک — چون فایل مشترک در دسترس نبود)، دکمه‌ی تولید خودکار نام‌کاربری/رمز،
  دکمه‌ی نمایش/مخفی‌کردن رمز
- ⚠️ جزئیات کامل فنی این دور (شامل نام دقیق فایل‌ها/متدها/مسیرهای API) توی
  `ROUND-54-CLASS-MANAGEMENT-UPGRADE.md` هست، نه این فایل

### دور ۵۳ — پوش‌نوتیفیکیشن (زیرساخت)
- مدل `PushToken`، ماژول `notifications/push.py` (ارسال واقعی از طریق سرویس
  Expo Push، بدون نیاز به Firebase Admin SDK در جنگو)
- دو endpoint جدید: ثبت و لغوِ توکن پوش
- تابع مرکزی `send_notification` در `utils.py` حالا خودکار پوش هم می‌فرسته —
  یعنی همه‌جای پروژه که از قبل این تابع رو صدا می‌زدن، بدون تغییر کد اضافه،
  پوش‌نوتیف هم می‌فرستن
- سمت موبایل: هوک `usePushNotifications`، متصل به `home.tsx` و منطق
  unregister هنگام خروج

---

## ۳. کارهای انجام‌شده در این نشست (جدا از تاریخچه‌ی بالا)

### ۳.۱ تکمیل و تست پوش‌نوتیفیکیشن
- Firebase پروژه ساخته شد، `google-services.json` دانلود و اضافه شد
- EAS راه‌اندازی شد (`eas init`, `eas credentials`, keystore ساخته شد)
- Development Build و Preview Build هر دو با موفقیت ساخته و نصب شدن
- ⚠️ **هنوز تأیید نهایی نشده که پوش واقعی به گوشی می‌رسه** — مشکوک‌ترین علت:
  کامپیوتری که جنگو روش اجرا می‌شه به `exp.host` (سرویس پوش Expo) دسترسی نداره
  (مشابه محدودیت `api.expo.dev` و `storage.googleapis.com` که در این پروژه
  بارها دیده شده). چون خطای شبکه در `push.py` فقط لاگ می‌شه نه نمایش داده،
  این مشکل بی‌صداست.

### ۳.۲ حل قطعی مشکل «Network Error» اپ موبایل
مشکل: اپ (چه development چه preview) موقع اتصال به بک‌اند جنگوی لوکال (روی
IP کامپیوتر، پروتکل http) با خطای اتصال مواجه می‌شد.

**چیزهایی که رد شدن (مشکل از اینا نبود):** IP اشتباه، فایروال ویندوز، نسخه‌ی
پکیج‌ها، `ALLOWED_HOSTS`، صحت commit، نصب مجدد اپ.

**علت واقعی:** از اندروید ۹ به بعد، `http://` ساده به‌صورت پیش‌فرض مسدوده.
تنظیم دستی اولیه (`usesCleartextTraffic: true` مستقیم در بلاک `android` در
`app.json`) کافی نبود.

**راه‌حل نهایی:** پلاگین رسمی Expo، `expo-build-properties`:
```json
["expo-build-properties", { "android": { "usesCleartextTraffic": true } }]
```
بعد از این، اتصال کاملاً برقرار شد (تأییدشده با پیام دقیق خطای دیباگ که کد
۴۰۱ داد، یعنی واقعاً به سرور رسیده بود، فقط رمز اشتباه بود).

### ۳.۳ بازطراحی UI/UX اپ موبایل
- منوی صفحه‌ی اصلی (`home.tsx`): از یه ردیف دکمه‌ی متنی شلوغ، به منوی آیکون‌دار
  (`@expo/vector-icons`) با گروه‌بندی بصری تبدیل شد؛ دکمه‌ی خروج فقط آیکون بالای
  هدر، «کلاس‌های گروهی» دکمه‌ی اصلی برجسته، بقیه گرید آیکونی با بج قرمز شمارشگر
- رفع مشکل نامرئی‌بودن **متن تایپ‌شده** در فیلدها (رنگ/پس‌زمینه صریح، مستقل از
  حالت تاریک/روشن گوشی) — در `index.tsx`, `request-form.tsx`, `satisfaction.tsx`
- رفع مشکل نامرئی‌بودن **متن راهنمای (placeholder)** فیلدها — در همون سه فایل
  به‌علاوه‌ی `entry-exit-requests.tsx` و `unregistered-student-form.tsx`

### ۳.۴ بازطراحی و رفع باگ پنل ادمین
- فونت فارسی استاندارد (Vazirmatn) اضافه شد — فقط با `@import` در `src/index.css`،
  بدون نیاز به نصب پکیج npm (چون کاربر هرگز `package.json` رو جایگزین نمی‌کنه)
- اندازه‌ی کلی متن‌ها با `zoom: 108%` روی `html` بزرگ‌تر شد
- تب‌های سه صفحه (`Dashboard.js`, `Followups.js`, `StaffMessages.js`) از حالت
  مستطیلی ساده به قرص‌مانند مدرن با سایه تغییر کردن
- خروجی اکسل/چاپ به صفحات **اساتید** (`TeacherManagement.js`) و **کارمندان**
  (`Employees.js`) اضافه شد (صفحه‌ی دانش‌آموزان از قبل داشت)
- رفع باگ: خروجی اکسل/چاپ کلاس‌های خصوصی (`Dashboard.js`) فیلدهای نام، نام‌خانوادگی،
  کد ملی، موبایل رو خالی می‌فرستاد — چون این اطلاعات توی `student_info` (یه
  فیلد تودرتو) ذخیره‌ست، نه مستقیم روی خودِ رکورد. کد اصلاح شد تا از
  `r.student_info.first_name` و مشابه بخونه (دقیقاً هماهنگ با چیزی که خودِ
  جدول توی صفحه استفاده می‌کنه)

### ۳.۶ پیکر تاریخ/ساعت شمسی برای اساتید (اپ موبایل)
مشکل: موقع ثبت «تاریخ و ساعت اتمام کلاس» توسط استاد، پیکر پیش‌فرض گوشی
(`@react-native-community/datetimepicker`) همیشه تقویم **میلادی** نشون می‌داد.

راه‌حل: دقیقاً با همون الگویی که قبلاً برای پنل ادمین ساخته شده بود (پیاده‌سازی
خالص جاوااسکریپت، بدون پکیج خارجی)، دو فایل جدید ساخته شد:
- `src/utils/jalali.ts` — الگوریتم تبدیل شمسی/میلادی (پورت‌شده از
  `admin-panel/src/utils/jalali.js`)
- `src/components/jalali-date-time-picker.tsx` — یه Modal با استپرهای روز/ماه
  (اسم فارسی)/سال و بعد ساعت/دقیقه

`home.tsx` (بخش «اتمام کلاس») به این کامپوننت جدید متصل شد. تاریخ نهایی که به
سرور می‌ره همچنان یه `Date` استاندارد جاوااسکریپته (میلادی، پشت‌صحنه) — فقط
نمایش/انتخاب برای استاد شمسیه.

⚠️ **`group-classes.tsx` هنوز از پیکر میلادی قدیمی استفاده می‌کنه** — اگه اونجا
هم لازمه شمسی بشه، باید با همین کامپوننت جدید (`jalali-date-time-picker.tsx`)
جایگزین بشه (هنوز انجام نشده).

### ۳.۸ تصمیم: بازیابی رمز عبور همچنان دستی می‌مونه
بررسی شد که آیا برای بازیابی رمز (فراموشی رمز) بهتره ایمیل یا پیامک خودکار
اضافه بشه. نتیجه: **فعلاً همین روش دستی فعلی (مدیر کد رو دستی به کاربر میگه)
باقی می‌مونه**، چون:
- ایمیل نیاز به SMTP (احتمالاً جیمیل) داره که با توجه به الگوی این پروژه
  (دسترسی‌نداشتن به دامنه‌های گوگل بدون VPN)، ریسک بی‌صدا کارنکردن داره —
  دقیقاً مثل مشکل احتمالی پوش‌نوتیفیکیشن
- پیامک نیاز به سرویس SMS ایرانی (هزینه‌بر) داره
- تصمیم گرفته شد فعلاً وقتش نیست؛ اگه بعداً (مثلاً بعد از دیپلوی روی VPS با
  اینترنت پایدار) خواستیم، می‌شه دوباره بررسی کرد

### ۳.۹ فیلدهای پرسنلی کامل استادها + شبا/کارت کارمندان + شماره ردیف همه‌جا

**بک‌اند:**
- `EmployeeProfile` (اپ payroll): فیلدهای `sheba_number`, `bank_account_number`, `card_number` اضافه شد (migration `payroll/0003`)
- `TeacherSerializer`: فیلدهای `father_name`, `national_code`, `birth_date`, `birth_date_jalali` که از قبل روی مدل `User` بودن ولی توی این سریالایزر نبودن، اضافه شدن

**پنل ادمین — مدیریت استادها:** فرم کامل شد با کد ملی (الزامی، ۱۰ رقم انگلیسی)، نام
پدر (الزامی فارسی)، تاریخ تولد (شمسی، با `JalaliDateInput`)، مدرک/رشته تحصیلی
(الزامی فارسی)، آدرس، وضعیت تاهل+تعداد فرزندان، شماره شبا+حساب بانکی. مثل
کارمندان، اطلاعات پرسنلی این‌ها هم توی همون مدل مشترک `EmployeeProfile` ذخیره
می‌شه (دو تماس API جدا: یکی `/api/teachers/` برای حساب کاربری، یکی
`/api/employee-profiles/` برای اطلاعات پرسنلی).

**پنل ادمین — کارمندان:** شماره شبا و شماره کارت اضافه شد. **یه باگ جانبی هم
پیدا و رفع شد:** خروجی اکسل/چاپ قبلاً مستقیم از خودِ رکورد کاربر می‌خوند
(`e.education_degree`, `e.hire_date`, ...) درحالی‌که این‌ها توی `EmployeeProfile`
جدا ذخیره‌ن — یعنی همیشه خالی می‌رفت. اصلاح شد تا از `profiles[e.id]` بخونه.

**دکمه‌ی نمایش/پنهان رمز عبور + تولید خودکار نام‌کاربری/رمز:** به فرم استادها،
کارمندان (که تولید خودکار از قبل داشت، فقط نمایش رمز اضافه شد)، و فرم ویرایش
دانش‌آموز اضافه شد. ابزار مشترک: `src/utils/credential-generator.js`

**شماره ردیف در خروجی اکسل/چاپ:** به همه‌ی صفحاتی که این خروجی رو داشتن اضافه
شد — `Dashboard.js` (از قبل داشت)، `Students.js`, `TeacherManagement.js`,
`Employees.js`, `Discounts.js`, `GroupClasses.js`, `LevelTests.js`, `Library.js`,
`NewLeads.js`, `Leaves.js`, `Payroll.js`. فقط `ClassManagement.js` عمداً دست‌نخورده
موند چون ستون «شماره» اونجا از قبل شناسه‌ی معنادار خودِ کلاسه (نه صرفاً ردیف).

### ۳.۱۰ بازطراحی کامل موتور محاسبه‌ی حقوق + فیش استاندارد

**بک‌اند (اپ payroll):**
- `SalaryProfile`: فیلد `housing_allowance_yearly` (حق مسکن سالانه) اضافه شد، با
  معادل ماهانه/روزانه/ساعتی خودکار (`housing_allowance_monthly` و `components_breakdown`)
- فیلد قدیمی `insurance_rate_30_days` **حذف** و جایگزین دو فیلد شد:
  `insurance_base_single` و `insurance_base_married` (مزد مبنای بیمه‌ی متفاوت
  برای مجرد/متاهل، طبق قانون)
- تابع `days_in_jalali_month(jy, jm)`: تعداد روز واقعی هر ماه شمسی (۲۹/۳۰/۳۱،
  با تشخیص صحیح سال کبیسه — تست شد که ۱۴۰۳ درست ۳۰ روزه شناسایی می‌شه). این
  جایگزین فرض ثابت قبلی (۲۲۰ ساعت استاندارد ماهانه، صرف‌نظر از طول واقعی ماه) شد
- `MonthlyPayroll`: فیلد `extra_payment` (اضافه‌پرداخت) اضافه شد، مستقیم به
  ناخالص اضافه می‌شه
- `insurance_amount` حالا = ۷٪ از (مزد مبنای متناسب با تاهل/تجرد، پرورده‌شده به
  تعداد روزهای بیمه‌ی همون ماه) — ثابت `EMPLOYEE_INSURANCE_RATE = 0.07`
- `hourly_wage`/`daily_wage` حالا بر اساس `days_in_month` واقعی همون ماه محاسبه
  می‌شن، نه فرض ثابت
- `approved_leave_days_this_month` / `approved_leave_hours_this_month`: خودکار
  از مدل `LeaveRequest` استخراج می‌شه (مرخصی تاییدشده‌ی همون ماه شمسی)
- تابع `number_to_persian_words()`: تبدیل عدد به حروف فارسی، از صفر نوشته شده
  (بدون هیچ پکیج خارجی)، برای نمایش خالص پرداختی در فیش
- `worked_hours` همچنان **دستی و قابل‌ویرایش** باقی موند — یادداشت صریح توی کد
  گذاشته شده که در آینده باید به یه صفحه‌ی «ثبت ورود و خروج» (که هنوز ساخته
  نشده) وصل بشه تا خودکار محاسبه بشه

**پنل ادمین (`Payroll.js`):**
- فرم حقوق پایه و فرم فیش ماهانه با همه‌ی فیلدهای جدید بالا کامل شد
- «تعداد روزهای بیمه» با انتخاب سال/ماه خودکار با `daysInJalaliMonth` هماهنگ
  می‌شه (قابل‌ویرایش دستی هم هست)
- دکمه‌ی جدید **«فیش رسمی»** (چاپ + اکسل) روی هر ردیف فیش ماهانه: طراحی چندبخشی
  دقیقاً طبق خواسته — اطلاعات هویتی+ماه، اقلام قانونی مصوب سالانه، کارکرد+مرخصیِ
  خودکارِ همون ماه، پرداختی‌ها و کسورات جزءبه‌جزء، خالص پرداختی به عدد و حروف،
  شماره شبا (از `EmployeeProfile`)، ردیف امضا. این دو تابع (`printOfficialPayslip`,
  `exportOfficialPayslip`) مستقیم و مجزا از ابزارهای عمومی `exportToExcel`/`printTable`
  نوشته شدن چون طراحی چندبخشی نیاز به کنترل کامل‌تری داشت

⚠️ **نکته‌ی مهم برای تست:** چون `insurance_rate_30_days` حذف شد، هر داده‌ی قبلی
که توی این فیلد بوده با migration پاک می‌شه — باید دوباره مبلغ مبنای بیمه‌ی
مجرد/متاهل برای هر `SalaryProfile` وارد بشه.

### ۳.۱۱ کلاس‌های فیزیکی، ثبت‌نام واقعی دانش‌آموز، پرداخت، و حضور و غیاب

**پس‌زمینه‌ی مهم:** سیستم قبلیِ `ClassSlot` (مدیریت کلاس‌ها) از قبل دقیقاً همون
ساختار روزهای زوج/فرد/پنجشنبه‌صبح/پنجشنبه‌عصر/جمعه با همون ساعت‌های خواسته‌شده رو
داشت — فقط باید ساخت دسته‌جمعی و ثبت‌نام واقعی دانش‌آموز بهش اضافه می‌شد
(قبلش `current_count` فقط یه عدد خام بود، هیچ دانش‌آموز واقعی‌ای بهش وصل نبود).

**ساعت‌های نهایی مورد استفاده (تأییدشده با کاربر):**
- زوج/فرد (سه‌روز‌در‌هفته): ۹:۴۵-۱۱:۱۵ / ۱۱:۳۰-۱۳:۰۰ / ۱۵:۴۵-۱۷:۱۵ / ۱۷:۳۰-۱۹:۰۰ / ۱۹:۰۰-۲۰:۳۰
- پنجشنبه‌صبح: ۸:۰۰-۱۳:۰۰ (یک بلوک)
- پنجشنبه‌عصر: ۱۳:۰۰-۱۷:۱۵ (یک بلوک، **جدا از چرخه‌ی فرد**، یک‌روز‌در‌هفته‌ی مستقل)
- جمعه: ۸:۳۰-۱۳:۱۵ (یک بلوک)

**بک‌اند (`class_management`):**
- `BulkCreatePhysicalClassesView` (`/api/class-management/bulk-create/`): برای
  هر شماره‌کلاس فیزیکی (۱ تا ۱۱)، ظرفیت + جنسیت گرفته و ۱۳ اسلات خودکار می‌سازه
  (۵ زوج + ۵ فرد + ۱ پنجشنبه‌صبح + ۱ پنجشنبه‌عصر + ۱ جمعه). تکراری‌ها رد می‌شن
  (idempotent، میشه چندبار زد)
- مدل جدید `ClassSlotEnrollment`: دانش‌آموز واقعی + کلاس + روش پرداخت (پوز/نقدی/
  درگاه/کارت‌به‌کارت/کیف‌پول) + مبلغ شهریه + کد پیگیری پوز (فقط اگه پوز) + تاریخ/
  ساعت شمسی خودکار
- `ClassSlotEnrollView`/`ClassSlotUnenrollView`/`ClassSlotRosterView`: ثبت،
  حذف، و گرفتن لیست دانش‌آموزان یک کلاس خاص
- `StudentQuickSearchView` (`/api/student-quick-search/`) در اپ accounts:
  جستجوی زنده‌ی دانش‌آموز با نام/نام‌خانوادگی/کدملی، برای autocomplete
- **جنسیت (`gender`) اضافه شد:**
  - به مدل `User` (خانم/آقا) — قابل‌ثبت هنگام ثبت‌نام دانش‌آموز
  - به مدل `ClassSlot` (دخترانه/پسرانه/مختلط) — پیش‌فرض بولک‌کریت: **زوج=دخترانه،
    فرد=پسرانه**؛ برای سه گروه یک‌روز‌در‌هفته، جنسیت هرکدوم جدا از مدیر پرسیده میشه
  - موقع ثبت‌نام دانش‌آموز در کلاس، تطابق جنسیت با کلاس چک می‌شه (مگر کلاس مختلط باشه)

**پنل ادمین (`ClassManagement.js`, `Students.js`):**
- دکمه‌ی «ساخت کلاس فیزیکی»: جدول ۱۱ردیفی (ظرفیت + جنسیت هر گروه تک‌روزه)
- لیست کلاس‌ها گروه‌بندی رنگی شد (۵ رنگ متفاوت برای ۵ گروه روز)
- روی هر کارت: دکمه‌ی ثبت دانش‌آموز (جستجوی زنده + انتخاب روش پرداخت + مبلغ +
  کد پوز در صورت نیاز) و دکمه‌ی لیست دانش‌آموزان (+ خروجی اکسل/چاپ حضور و غیاب:
  ۵ ستون هویتی + ۱۵ ستون جلسه + ۱ ستون توضیحات عریض)
- فیلد جنسیت به فرم افزودن/ویرایش دستی کلاس، و به فرم ثبت‌نام/ویرایش دانش‌آموز اضافه شد
- شماره ردیف به خروجی کلی این صفحه هم اضافه شد (تنها صفحه‌ای که قبلاً عمداً جا
  مونده بود، چون ستون «شماره» قبلاً شناسه‌ی خودِ کلاس بود، نه ردیف)

⚠️ **نکته‌ی مهم برای تست:** چون فایل `ClassManagement.js` این بار با یه اسکریپت
خودکار (نه دستی) بازنویسی و یه تابع بزرگ (`renderSlotCard`) ازش استخراج شد، حتماً
بعد از جایگزینی، صفحه رو کامل تست کن (باز/بسته‌شدن فرم‌ها، ثبت‌نام واقعی، خروجی‌ها)
تا مطمئن بشیم هیچ بخشی حین این جراحی گم نشده.




- VPS خریداری شد: **پارس‌پک، پلن irVPS3** (۲ گیگ رم، ۲ هسته، ۵۰ گیگ SSD)، Ubuntu
- هنوز هیچ‌چیزی روی VPS نصب/دیپلوی نشده؛ دامنه هم هنوز خریداری نشده

---

## ۴. کارهای ناتمومِ فوری (اولویت بالا)

- [ ] **migration دور ۶۰ هنوز روی سیستم کاربر اجرا نشده** — این دور واقعاً مدل/فیلد
  تازه داره (`LevelRenewalApproval` در `class_management`، فیلد `gender` در
  `level_tests`):
  `python manage.py makemigrations class_management level_tests` و `migrate`
- [ ] **صفحه‌ی «افراد دارای تخفیف» (`Discounts.js`) هنوز گرفته نشده** — بک‌اندش
  (`GET /api/class-management/discounted-persons/`) از دور ۵۴ کار می‌کنه و خودکار
  پر می‌شه، فقط فایل فرانتش گرفته نشده
- [ ] **درگاه پرداخت واقعی** — فعلاً فقط UI و پیام «به‌زودی»؛ کاربر گفته بعداً
  برای «ثبت کلاس خصوصی توسط دانش‌آموز» هم لازمه — نیاز به تصمیم روی provider
  (زرین‌پال/آیدی‌پی/...) + merchant ID
- [ ] **سوابق کلاس خصوصی/گروهی/ورکشاپ در صفحه‌ی «سوابق آموزشی»** — نیاز به
  فایل‌های اپ‌های `classes` و `group_classes` که هنوز گرفته نشده
- [ ] **فلوتینگ‌کردن هدر بقیه‌ی صفحات پنل ادمین** (Payroll، Leave، و هرچی دیگه‌ست) —
  فقط صفحاتی که فایلشون گرفته شده (`ClassManagement`, `Students`,
  `TeacherManagement`, `Library`) این تغییر رو گرفتن
- [ ] **صفحه‌ی مجزای پروفایل مدیر آموزش** — اگه وجود داره و کاربر می‌خواد پنل
  «تایید تمدید سطح» به‌جای `ClassManagement.js` اونجا باشه، فایلش لازمه
- [ ] **بررسی علت پاک‌شدن یوزر/رمز ادمین با هر بار اجرای `runserver`** — فرضیه‌ی
  اصلی: `DATABASES` در `core/settings.py` احتمالاً روی `:memory:` تنظیمه به‌جای
  فایل دائمی SQLite. باید محتوای این بخش بررسی بشه (کاربر هنوز نفرستاده)
- [ ] بررسی اینکه آیا `PushToken` واقعاً توی پنل ادمین جنگو ثبت می‌شه، و آیا
  پوش‌نوتیف واقعاً به گوشی می‌رسه یا نه
- [ ] تصمیم و اجرای اسم جدید دلخواه برای پنل ادمین و اپ (کاربر گفته اسم داره
  ولی هنوز اعلام نکرده)
- [ ] لوگوی دلخواه (آیکون اصلی اپ + نسخه‌ی ساده‌ی تک‌رنگ برای نوار اعلان) —
  کاربر هنوز طراحی نکرده، بعداً دنبال می‌شه

## ۵. کارهای خواسته‌شده ولی هنوز شروع‌نشده

- [ ] لوگو توی صفحه‌ی ورود اپ موبایل
- [ ] نمایش نسخه‌ی اپ + نام منتشرکننده به‌صورت قابل‌مشاهده داخل خودِ اپ
- [ ] Deploy واقعی بک‌اند روی VPS پارس‌پک (نصب پایتون/جنگو/دیتابیس/Gunicorn/Nginx)
- [ ] خرید دامنه و اتصال DNS به سرور
- [ ] فعال‌سازی SSL (Let's Encrypt)
- [ ] تغییر آدرس API اپ از IP لوکال به دامنه‌ی واقعی https + build نهایی
- [ ] `group-classes.tsx` هنوز پیکر تاریخ میلادی داره — باید با کامپوننت
  `jalali-date-time-picker.tsx` جدید هماهنگ بشه (اگه لازم بود)
- [ ] پاک‌سازی پوشه‌های تکراری/اضافی توی ریشه‌ی ریپوی موبایل (یه پوشه‌ی `app`
  جدا از `src/app`، و یه پوشه‌ی دیگه با فاصله در اسمش: "language school app")

---

## ۶. نکات فنی حیاتی (برای جلوگیری از تکرار اشتباهات قبلی)

### قوانین دیپلوی و ساختار فایل
- **بک‌اند و پنل ادمین:** کاربر همیشه فقط کد رو جایگزین می‌کنه، هرگز
  `package.json`/`requirements.txt` رو خودش عوض نمی‌کنه — پس هر پکیج جدیدی
  باید صریحاً اعلام و کاربر مطلع بشه، هرگز silently اضافه نشه
- **اپ موبایل:** همون قانون — کاربر پوشه‌ی `src/` رو جایگزین می‌کنه، ولی
  `app.json`/`package.json`/`eas.json` رو جدا و با نظارت من عوض می‌کنه
- برای دریافت فایل از کاربر جهت زیپ‌کردن، فقط پوشه‌های لازم رو بخواه (جزئیات
  کامل توی حافظه‌ی Claude ثبت شده — بک‌اند: فایل‌های `.py` بدون migrations؛
  پنل ادمین: کل `src/`؛ موبایل: کل `src/` + سه فایل تنظیمات ریشه)

### محدودیت شبکه (بسیار مهم، هر بار تکرار شده)
- کامپیوتر توسعه‌دهنده **بدون VPN** به این دامنه‌ها دسترسی نداره:
  `api.expo.dev`, `storage.googleapis.com`, `developer.android.com`، احتمالاً
  `exp.host`
- **VPN هم فعلاً روی این کامپیوتر کار نمی‌کنه** (چند بار امتحان و ناموفق بود)
- برای build همیشه از **«Build from GitHub»** در داشبورد وب expo.dev استفاده
  کن، نه از `eas build`/`eas update` محلی (این‌ها همیشه با خطای ۴۰۳ مواجه
  می‌شن، حتی وقتی صفحه‌ی وب expo.dev درست کار می‌کنه)
- برای دیدن خطای واقعی build، دکمه‌ی «Copy error as prompt» معمولاً فقط
  متادیتای کلی build رو میده، نه لاگ واقعی — باید مستقیم توی بخش Logs دنبال
  مرحله‌ای با × قرمز گشت

### شناسه‌های فنی مهم پروژه
- Package name اندروید: `com.languageschool.app`
- نام اپ (نمایشی): My Language School
- EAS project ID: `1ba99ba7-db51-4728-b7c3-cad26e1af0f9`
- VPS: پارس‌پک، پلن irVPS3

### الگوهای رایج خطای build که قبلاً رخ داده (برای تشخیص سریع‌تر دفعات بعد)
- فایل آیکون تعریف‌شده در `app.json` که واقعاً توی `assets/images` وجود نداره
  → fail سریع و بی‌صدا
- `projectId` جایگزین‌نشده (placeholder مونده) در `app.json`
- ناهماهنگی نسخه‌ی پکیج‌های Expo بعد از نصب پکیج جدید → با
  `npx expo install --fix` حل می‌شه
- اسم غلط پکیج موقع نصب دستی (مثل `vector-icon` به‌جای `vector-icons`)
- استفاده از یه پکیج جدید (مثل `@expo/vector-icons`) بدون نصب صریح، حتی اگه
  «باید» از قبل با Expo بیاد — همیشه `npx expo install <package>` رو بزن

---

## ۷. فایل‌های یادآور مرتبط (برای مطالعه‌ی تکمیلی)
- `ROUND-57-60-DIRECT-ENROLLMENT-WORKFLOW.md` — ثبت‌نام مستقیم (وب+اپ)، پیشنهاد
  هوشمند سطح، گردش‌کار تایید تمدید، ثبت‌نام‌های در انتظار، اتصال تعیین‌سطح — **جدیدترین**
- `ROUND-55-56-CLASS-MANAGEMENT-BUGFIXES.md` — رفع باگ تخصیص/ثبت‌نام، تفکیک کلاس، سرچ کتابخانه
- `ROUND-54-CLASS-MANAGEMENT-UPGRADE.md` — شهریه/تخفیف/استرداد/کیف‌پول
- `PROJECT-FULL-CONTEXT.md` — جزئیات کامل فنی مدل‌ها/API/کد (به‌روز تا دور ۵۲)
- `PUSH-NOTIFICATIONS-SETUP-PROGRESS.md` — جزئیات کامل مراحل راه‌اندازی پوش
- `DEPLOY-SETUP-PROGRESS.md` — جزئیات خرید/راه‌اندازی VPS و دامنه
- `NETWORK-ERROR-REPORT.md` — گزارش کامل عیب‌یابی مشکل اتصال شبکه (برای مرجع
  تاریخی، چون خودِ مشکل الان حل شده)
- `PROJECT-STATUS-AFTER-NETWORK-FIX.md` — وضعیت بلافاصله بعد از حل مشکل شبکه
