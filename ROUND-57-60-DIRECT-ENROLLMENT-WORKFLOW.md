# فایل بازیابیِ جامع — دورهای ۵۷ تا ۶۰: ثبت‌نام مستقیم، پیشنهاد سطح هوشمند، گردش‌کار تایید

این فایل مکمل `ROUND-54-CLASS-MANAGEMENT-UPGRADE.md` و `ROUND-55-56-CLASS-MANAGEMENT-BUGFIXES.md`
است. اگه چت جدید باز شد، این سه فایل با هم دقیق‌ترین مرجع فنیِ ماژول «مدیریت کلاس» هستن.

---

## ۱. ویژگی بزرگ: «ثبت‌نام مستقیم دانش‌آموز» (دور ۵۷)

خواسته‌ی اصلی: یه بخش که با سرچ دانش‌آموز، سطح مناسبش رو خودکار پیشنهاد بده و
مستقیم ثبت‌نامش کنه — هم توی پنل وب (برای ادمین/آفیس) هم توی اپ (خودِ دانش‌آموز، با
عنوان «ثبت‌نام دوره ترمیک»).

### بک‌اند (`class_management`)
- `ClassSlotEnrollment` فیلدهای تازه گرفت: `receipt_image` (تصویر رسید کارت‌به‌کارت)،
  `self_enrolled` (آیا خودِ دانش‌آموز از اپ ثبت‌نام کرده)، `payment_verified`
  (پیش‌فرض `True` برای ثبت‌نام‌های دستیِ ادمین؛ `False` برای کارت‌به‌کارتِ خودِ دانش‌آموز
  تا مدیر تایید کنه)
- `DirectEnrollSuggestionsView` (ادمین) و `MyDirectEnrollSuggestionsView` (خودِ
  دانش‌آموز) — هر دو از تابع مشترک `_compute_level_suggestion(student)` استفاده
  می‌کنن (پایین توضیح داده شده)
- `SelfEnrollView` (`POST /api/class-management/slots/<pk>/self-enroll/`) —
  خودثبت‌نامیِ اپ؛ فقط کارت‌به‌کارت (با رسید اجباری) یا درگاه (فعلاً غیرفعال، پیام
  «به‌زودی»)؛ مبلغ/تخفیف را خودِ سرور حساب می‌کند، از کلاینت اعتماد نمی‌کند
- `VerifyEnrollmentPaymentView` / `RejectPendingEnrollmentView` — تایید/رد رسید توسط ادمین
- `MyEnrollmentsView` (`GET /api/class-management/my-enrollments/`) — لیست
  دوره‌های ترمیکِ خودِ دانش‌آموز، برای نمایش توی اپ

### اپ (`language-school-app`)
- صفحه‌ی تازه‌ی `src/app/direct-enrollment.tsx` — «ثبت‌نام دوره ترمیک»: سرچ نداره
  (چون خودِ کاربره)، سطح پیشنهادی + کلاس‌های مجاز + قیمت/تخفیف رو نشون می‌ده،
  آپلود رسید کارت‌به‌کارت دقیقاً با الگوی موجود پروژه (`request-form.tsx`، تابع
  `appendReceipt` که هم وب هم گوشی واقعی رو پشتیبانی می‌کنه)
- `home.tsx`: دکمه‌ی ورود به این صفحه (فقط نقش دانش‌آموز) + بخش «دوره‌های ترمیک من»
  (از `my-enrollments/`) با نشان تاییدشده/در انتظار

### وب (`ClassManagement.js`)
- پنل «🆕 ثبت‌نام مستقیم دانش‌آموز»: سرچ+خودتکمیل، سطح پیشنهادی، لیست کلاس‌های
  مجاز، همه‌ی روش‌های پرداخت (کارت‌به‌کارت/پوز/نقدی/کیف‌پول)، تخفیف

---

## ۲. منطق مشترک پیشنهاد سطح + گردش‌کار تایید تمدید (دور ۵۹)

### مشکل گزارش‌شده
پیشنهاد سطح قبلی خیلی ساده بود — نه اعتبار زمانی چک می‌کرد، نه سطح پایانی گروه سنی رو،
و امکان اصلاح دستی/ورودی جدید نداشت.

### راه‌حل: `_compute_level_suggestion(student)` در `class_management/views.py`
تابع مرکزی که همه‌جا (ادمین، اپ، `SelfEnrollView`، `StudentEducationHistoryView`)
استفاده می‌شه:
1. سطح رو از نزدیک‌ترین منبع می‌گیره: آخرین `ClassSlotEnrollment` واقعی یا آخرین
   `LevelTest` تکمیل‌شده — هرکدوم تاریخش جدیدتر بود
2. **اعتبار ۶۰ روزه:** اگه بیش از ۶۰ روز از اون منبع گذشته، `needs_retest=True`،
   `retest_reason='expired'`
3. **سطح پایانی گروه سنی:** اگه سطح جزو `MAX_LEVELS_NEEDING_RETEST = {'i5', 'teen15', 'teen 15'}`
   باشه، `needs_retest=True`، `retest_reason='max_level_reached'` (این یکی
   قابل‌دورزدن با تایید مدیر آموزش **نیست** — همیشه باید تعیین‌سطح بشه)
4. اگه `LevelRenewalApproval` تاییدشده برای همون دانش‌آموز+سطح وجود داشته باشه،
   هشدار انقضا (نه سطح پایانی) خودکار برداشته می‌شه

### مدل جدید: `LevelRenewalApproval`
وقتی اعتبار ۶۰روزه تموم شده، به‌جای تعیین‌سطح مجدد، ادمین می‌تونه درخواست تایید
برای مدیر آموزش بفرسته:
- `POST /api/class-management/level-renewal-requests/` (ادمین می‌سازه)
- `GET /api/class-management/level-renewal-requests/?status=pending` (مدیر آموزش می‌بینه)
- `POST /api/class-management/level-renewal-requests/<pk>/decide/` با `{decision: 'approve'|'reject'}`
- فرانت: پنل «🎓 تایید تمدید سطح» توی `ClassManagement.js` (چون فایل پروفایل
  مجزای مدیر آموزش رو نداشتم؛ اگه اون فایل رو بفرستی، می‌تونم منتقلش کنم)

### قابلیت‌های اضافه‌شده به پنل «ثبت‌نام مستقیم» (وب)
- **سطح دستی:** اگه پیشنهاد خودکار غلط بود، از یه `<select>` (نه تایپی — طبق
  خواسته‌ی صریح دور بعد) سطح رو عوض کن؛ کلاس‌های اون سطح دوباره لیست می‌شن
- **ورودی جدید:** اگه دانش‌آموز اصلاً پیدا نشد، دکمه‌ی «+ ورودی جدید» یه
  مینی‌فرم (دقیقاً فیلدهای فرم افزودن دانش‌آموز) باز می‌کنه؛ بعد از ثبت (که از
  همون `POST /api/students/` استفاده می‌کنه)، مستقیم می‌ره سراغ پیشنهاد سطح
- **نشان تخفیف خودکار:** «🏷 این فرد تخفیف خودکار داره: X%»

⚠️ همه‌ی این‌ها (بجز سطح دستی و ورودی جدید که کار ادمینه) توی اپ هم هست — فقط
به‌صورت نمایشی/هشداری، بدون امکان دورزدن.

---

## ۳. رفع باگ‌های اساسی (دور ۵۹-۶۰)

### تخصیص خودکار vs ثبت‌نام واقعی (یادآوری از دور ۵۶، تکمیل‌شده)
`real_enrolled_count` (property زنده، نه فیلد) = `self.enrollments.filter(payment_verified=True).count()`
— یعنی الان **دو لایه** فیلتر داره:
1. جدا از `current_count` (که فقط برای ابزارهای مازاد/تخصیص می‌مونه)
2. فقط ثبت‌نام‌های **تاییدشده** رو می‌شمره — ثبت‌نامِ کارت‌به‌کارتِ در-انتظار (دور ۶۰)
   جزوش نیست تا وقتی تایید بشه

### ثبت‌نام‌های در انتظار تایید (دور ۶۰، خواسته‌ی ۱)
- `ClassSlotRosterView` فقط `payment_verified=True` برمی‌گردونه — یعنی ثبت‌نام
  در-انتظار توی «لیست دانش‌آموزان کلاس» دیده نمی‌شه
- `PendingSelfEnrollmentsView` (`GET /api/class-management/pending-self-enrollments/`)
  — لیست کامل همه‌ی در-انتظارها از **همه‌ی کلاس‌ها یک‌جا**
- فرانت: مودال مرکزی که هر بار وارد صفحه‌ی مدیریت کلاس‌ها می‌شی خودکار باز می‌شه
  (اگه چیزی در انتظار باشه) — مشخصات کامل + تصویر رسید + دکمه‌ی تایید/رد + دکمه‌ی بستن

### یکتایی کد ملی بین کلاس‌ها (دور ۶۰، خواسته‌ی ۲)
هم `ClassSlotEnrollView.post` هم `SelfEnrollView.post`: قبل از ساخت ثبت‌نام تازه،
چک می‌کنن آیا همین دانش‌آموز جای دیگه‌ای (کلاس دیگه) ثبت‌نام فعال داره — اگه آره،
خطا می‌دن (باید اول از «انتقال کلاس» استفاده کنن)

### سطوح کودک ناقص بودن (یادآوری دور ۵۸، تکمیل‌شده)
`infer_age_group_from_level` الان همه‌ی پیشوندهای واقعی کودک رو می‌شناسه:
`e, s, g, u, m, h, i` + عدد (نه فقط `e`/`i` که قبلاً بود)

### سطوح سفارشی (دور ۶۰، خواسته‌ی ۹)
`TuitionSettingListView.create()`: اگه `infer_age_group_from_level` جواب نده
(سطح غیراستاندارد)، گروه سنیِ دستیِ ارسالی از کلاینت رو قبول می‌کنه (به‌جای خطا).
فرانت: بخش «سطوح سفارشی» زیر جدول استاندارد، با دکمه‌ی «+ افزودن سطح سفارشی».

---

## ۴. سوابق آموزشی (دور ۶۰، خواسته‌ی ۳)
`StudentEducationHistoryView` (`GET /api/class-management/students/<id>/education-history/`):
سطح فعلی (همیشه زنده از `_compute_level_suggestion`) + تاریخچه‌ی کامل ثبت‌نام‌های
ترمیک (سطح، روش ثبت‌نام، وضعیت تایید، تاریخ شمسی). فرانت: دکمه‌ی «📚 سوابق آموزشی»
کنار «📊 سوابق مالی» توی `Students.js`.

⚠️ **کلاس‌های خصوصی/گروهی/ورکشاپ توی این صفحه نیست** — نیاز به فایل‌های اپ‌های
`classes` و `group_classes` دارم که هنوز نگرفتم.

---

## ۵. رابط کاربری (دور ۶۰، خواسته‌های ۴، ۶، ۷، ۸)

- **فلوتینگ کردن نوار بالا:** `header` توی `ClassManagement.js`, `Students.js`,
  `TeacherManagement.js`, `Library.js` همه `position: sticky; top: 0` شدن. کادر
  فیلتر روز توی `ClassManagement.js` هم فلوتینگه (`top: 62px`، زیر هدر).
  ⚠️ صفحات دیگه‌ای که فایلشون رو ندارم (Payroll، و غیره) این تغییر رو نگرفتن.
- **مخفی‌سازی پیش‌فرض آمار/تخصیص:** «📊 آمار دقیق کلاس‌ها» و «🧮 تخصیص خودکار
  کلاس‌ها» دو دکمه‌ی جدا شدن، پیش‌فرض بسته (`showStatsSection`/`showAllocateSection`)
- **تاریخ جلسه:** یه فیلد شمسی سراسری بالای لیست («📅 تاریخ جلسه‌ی امروز» + دکمه‌ی
  «اعمال به همه») + یه فیلد جدا زیر هر کارت کلاس؛ مقدارش مستقیم میره توی عنوان
  خروجی اکسل/چاپ حضور و غیاب همون کلاس (`sessionDates` state، کلید = id کلاس)
- **مبلغ پیش‌فرض کم‌رنگ:** توی فرم ثبت‌نام تک‌کلاسی، `enrollTuitionTouched` تا
  دست نخورده باشه، فیلد مبلغ خاکستری/ایتالیک نشون داده می‌شه؛ به‌محض تایپ، حالت
  عادی برمی‌گرده

---

## ۶. اتصال تعیین‌سطح به لیست دانش‌آموزان (دور ۶۰، تکمیل نهایی)

فایل‌های `level_tests/{models,views,serializers,urls}.py` گرفته شد و بررسی شد.

### چی اضافه شد
- فیلد `gender` روی مدل `LevelTest` (قبلاً نبود — لازم بود چون ساخت حساب کاربری
  بدون جنسیت ممکن نیست)
- توی `LevelTestDetailView.patch`: وقتی `status` به `completed` می‌رسه (یعنی هم
  `age_group` هم `level` پر شدن) و هنوز `student` FK خالیه:
  1. اول با `national_code` دنبال یه `User(role='student')` موجود می‌گرده — اگه
     بود، فقط وصلش می‌کنه (تکراری نمی‌سازه)
  2. اگه نبود و `first_name`+`last_name`+`national_code`+`gender` پر بودن، با
     همون `StudentSerializer` (از اپ `accounts` — دقیقاً همون مسیری که ثبت
     دستیِ دانش‌آموز از پنل استفاده می‌کنه) یه حساب تازه می‌سازه و وصل می‌کنه
  3. اگه به هر دلیل نامعتبر بود (مثلاً کد ملی تکراریِ غیردانش‌آموز)، بی‌صدا رد
     می‌شه — ثبت نتیجه‌ی تعیین‌سطح به‌خاطر این خطا مسدود نمی‌شه
- `LevelTests.js`: فیلد جنسیت (الزامی) به فرم ثبت داوطلب اضافه شد؛ نشان «✓ متصل
  به حساب دانش‌آموز» جلوی هر رکورد که وصل شده

---

## ۷. کارهای باز (برای ادامه‌ی دور بعد)

- [ ] **migration این دور واقعی و مهمه** — `LevelRenewalApproval` (مدل تازه در
  `class_management`) + فیلد `gender` (تازه در `level_tests`):
  ```
  python manage.py makemigrations class_management level_tests
  python manage.py migrate
  ```
- [ ] **صفحه‌ی «افراد دارای تخفیف» (`Discounts.js`) هنوز گرفته نشده** — بک‌اندش
  از دور ۵۴ کار می‌کنه
- [ ] **درگاه پرداخت واقعی** — کاربر گفته بعداً برای «ثبت کلاس خصوصی توسط دانش‌آموز»
  هم لازمه؛ نیاز به تصمیم روی provider (زرین‌پال/آیدی‌پی/...) + merchant ID
- [ ] **سوابق کلاس خصوصی/گروهی/ورکشاپ در سوابق آموزشی** — نیاز به فایل‌های اپ‌های
  `classes` و `group_classes`
- [ ] **صفحه‌ی مجزای پروفایل مدیر آموزش** — اگه وجود داره و می‌خوای پنل «تایید
  تمدید سطح» به‌جای `ClassManagement.js` اونجا باشه، فایلش رو بفرست
- [ ] **فلوتینگ‌کردن هدر بقیه‌ی صفحات پنل ادمین** (Payroll، Leave، و هرچی دیگه‌ست)
  — فقط صفحاتی که فایلشون رو دارم (`ClassManagement`, `Students`, `TeacherManagement`,
  `Library`) این تغییر رو گرفتن
- [ ] مورد ۱۱ در لیست دور آخر کاربر نبود (از ۱۰ مستقیم رفت به ۱۲) — اگه عمدی
  نبوده و موردی جا افتاده، دوباره بگه
