# فایل جامع شناسایی و بازیابی پروژه — سیستم مدیریت آموزشگاه زبان

این فایل کامل‌ترین مرجع پروژه‌ست؛ هدفش اینه که هر Claude (یا هر توسعه‌دهنده‌ی دیگه‌ای)
با خوندن همین یه فایل، کل وضعیت پروژه رو از صفر تا الان بفهمه و بتونه دقیقاً از
همین‌جا ادامه بده. این رو همیشه به‌روز نگه‌دار و کنار بقیه‌ی فایل‌های یادآور
(`PROJECT-FULL-CONTEXT.md`، فایل‌های `ROUND-*.md`، فایل پوش‌نوتیفیکیشن، فایل Deploy)
توی گیت‌هاب پوش کن.

---

## ۱. معرفی کلی پروژه

سیستم مدیریت یک آموزشگاه خصوصی زبان، متشکل از سه بخش به‌هم‌وصل:
- **بک‌اند:** Django REST Framework (ریپو: `language_school`)
- **پنل ادمین:** React (Create React App، ریپو: `admin-panel`) — برای مدیر، استاد، و کارمندان اداری
- **اپ موبایل:** Expo / React Native SDK 56 (ریپو: `language-school-app`) — برای استاد و دانش‌آموز

همه‌ی زبان رابط کاربری (UI)، برچسب فیلدها، و پیام‌های خطا **فارسی** هستن.

سه نقش اصلی کاربری: **ادمین**، **استاد** (و نقش‌های شبیه‌استاد: `evaluator`/مدیر
آموزش، `office`/کارمند اداری)، **دانش‌آموز**.

سه فاز کاری پروژه (از ابتدا):
- **فاز ۱:** کلاس‌های خصوصی، با گردش‌کار ارجاع دو مرحله‌ای (ادمین → استاد → تکمیل)
- **فاز ۲:** کلاس‌های گروهی/ورکشاپ
- **فاز ۳:** تعیین سطح، با پنل جدای ارزیاب

**اپ‌های جنگو شناخته‌شده تا الان:** `accounts` (کاربران، `ClassRequest` کلاس‌های
خصوصی)، `classes` (جلسات کلاس خصوصی)، `class_management` (کلاس‌های فیزیکی/ترمیک —
بزرگ‌ترین و پویاترین بخش پروژه)، `core` (تنظیمات اصلی جنگو)، `group_classes`
(کلاس‌های گروهی/ورکشاپ)، `leads`، `level_tests` (تعیین‌سطح + سطوح استاندارد)،
`library` (کتابخانه)، `notifications`، `payroll`، `staff_messages`.

### قوانین ثابت و همیشگی (خیلی مهم)
- کاربر همیشه فقط **کد رو جایگزین** می‌کنه؛ هرگز `package.json`/`requirements.txt`
  رو خودش عوض نمی‌کنه — هر پکیج جدیدی باید صریحاً اعلام بشه، هرگز silently اضافه نشه
- بک‌اند: فقط فایل‌های `.py` بدون `migrations`/`__pycache__`/`venv` لازمه
- پنل ادمین: کل پوشه‌ی `src/` کافیه
- اپ موبایل: کل پوشه‌ی `src/` کافیه (به‌علاوه‌ی `app.json`/`package.json`/`eas.json`
  فقط اگه لازم شد و با نظارت صریح)
- بعد از هر تغییر بک‌اندی که مدل/فیلد تازه اضافه کرده: همیشه یادآوری
  `python manage.py makemigrations <app> && python manage.py migrate`
- زبان همه‌جای پروژه، همیشه فارسی

---

## ۲. تاریخچه‌ی کامل توسعه (خلاصه‌ی همه‌ی دورها، به ترتیب زمانی)

### پایه‌گذاری اولیه
- رفع باگ اولیه‌ی متد `save()` (فراموش‌شدن `**kwargs`)
- گردش‌کار ارجاع دو مرحله‌ای استاد برای کلاس خصوصی
- ساخت صفحات موبایل از صفر (ثبت‌نام دانش‌آموز، درخواست کلاس با آپلود عکس رسید،
  دکمه‌های اقدام استاد، امتیازدهی رضایت)
- اصل معماری: هر فاز یه اپ جنگوی مستقل، با `notifications/utils.py` مشترک برای
  نوتیف‌های داخلی
- ساخت الگوی `PROJECT-FULL-CONTEXT.md` برای بازیابی کامل پروژه از گیت‌هاب

### دورهای ۱۹ تا ۴۱ (میان‌مدت)
- نقش ارزیاب (`مدیر آموزش`) با `User.TEACHER_LIKE_ROLES`
- حضور و غیاب چندجلسه‌ای برای کلاس‌های گروهی
- قیمت‌گذاری هر جلسه با سهم قابل‌تنظیم استاد/آموزشگاه
- `GroupPriceSetting` (تک‌نمونه‌ای/Singleton) با override هر ورکشاپ
- کامپوننت تاریخ جلالی (Jalali) به‌جای همه‌ی تقویم‌های میلادی — پیاده‌سازی خالص
  جاوااسکریپت، بدون پکیج خارجی
- از دور ۲۷ به بعد: build پنل ادمین با `CI=true npx react-scripts build` تست
  می‌شه تا خطاهای واقعی کامپایل دیده بشن
- بعد از هر deploy تازه: همیشه `python manage.py migrate` و
  `python manage.py reset_admin` لازمه

### دورهای ۴۲ تا ۵۲
- مدیریت کلاس مازاد (surplus) با منطق توزیع خودکار
- ماژول کامل حقوق و دستمزد (پروفایل کارمند، پروفایل حقوق، حقوق ماهانه با محاسبه‌ی
  بیمه، اضافه‌کاری/غیبت/پاداش، تأیید فیش حقوقی توسط کارمند، `printOfficialPayslip`/
  `exportOfficialPayslip` مجزا)
- سیستم مرخصی با سهمیه‌ی ساعتی ماهانه و سه دسته‌ی مرخصی
- تخصیص خودکار کلاس با ترجیحات اختیاری استاد/روز/زمان هر سطح
- گردش‌کار مجوز ورود/خروج با ارجاع ادمین‌به‌استاد
- نقش جدید `office` (کارمند اداری)
- ادغام اولیه‌ی تعیین سطح با ثبت‌نام مستقیم دانش‌آموز
- چاپ در همه‌ی صفحات با روش HTML-to-.xls بدون وابستگی خارجی

**باگ‌های مهم رفع‌شده در این بازه:** شکست deploy به‌خاطر پکیج npm جدید (`xlsx`) —
قانون «فقط `src/`» از همین‌جا ثابت شد؛ باگ `JalaliDateInput`؛ از دست‌رفتن داده‌ی
دانش‌آموز مازاد حین تأیید نهایی کلاس؛ فیلد FK گم‌شده‌ی `student` در
`LevelTestIntakeSerializer`.

### دور ۵۳ — پوش‌نوتیفیکیشن (زیرساخت) + رفع Network Error + بازطراحی UI
- مدل `PushToken`، `notifications/push.py` (ارسال از طریق Expo Push Service،
  بدون Firebase Admin SDK)؛ تابع مرکزی `send_notification` خودکار پوش هم می‌فرسته
- **رفع قطعی «Network Error» اپ:** علت واقعی، مسدودشدنِ `http://` ساده از اندروید
  ۹ به بعد بود؛ راه‌حل نهایی پلاگین `expo-build-properties` با
  `usesCleartextTraffic: true` (نه تنظیم دستی در بلاک android که کافی نبود)
- بازطراحی `home.tsx` به منوی آیکون‌دار؛ رفع نامرئی‌بودن متن/placeholder فیلدها
- فونت Vazirmatn، تب‌های قرص‌مانند، خروجی اکسل/چاپ به صفحات اساتید/کارمندان
- ماژول کامل مدیریت کلاس‌های فیزیکی/ترمیک متولد شد: `BulkCreatePhysicalClassesView`،
  مدل `ClassSlotEnrollment`، `StudentQuickSearchView`، فیلد جنسیت روی `User` و
  `ClassSlot`، ساخت دسته‌جمعی ۱۳ اسلات به‌ازای هر شماره‌کلاس فیزیکی

### دور ۵۴ — شهریه/تخفیف/استرداد/کیف‌پول
- `TuitionSetting` (شهریه‌ی مصوب سطح×گروه‌سنی)، `DiscountedPerson` (افراد دارای
  تخفیف، خودکار)، `EnrollmentRefund` (استرداد با شماره‌کارت/گیرنده/تاریخ خودکار)،
  `WalletTransaction` + `wallet_balance` روی `User`
- سه دکمه روی هر دانش‌آموزِ ثبت‌نام‌شده: استرداد، انتقال کلاس، انتقال به کیف پول
- بازطراحی لیست کلاس‌ها: ستون‌بندی ساعتی، فونت درشت سطح، فیلترهای پیشرفته
- `Students.js`: پیکر تاریخ تولد، تولید خودکار نام‌کاربری/رمز (بعداً در دور ۵۵ به
  ابزار مشترک `JalaliDateInput`/`credential-generator.js` وصل شد)

### دور ۵۵ و ۵۶ — رفع باگ ریشه‌ای «تخصیص خودکار vs ثبت‌نام واقعی» + تفکیک کلاس
- **باگ ریشه‌ای:** `current_count` هم برای عدد انتزاعیِ تخصیص خودکار هم برای
  شمارش ثبت‌نام واقعی استفاده می‌شد → بلافاصله بعد از تخصیص، اولین ثبت‌نام واقعی
  خطای «ظرفیت تکمیل» می‌داد. رفع شد با property های زنده‌ی جدید:
  `real_enrolled_count`/`real_capacity_status`/`real_seats_left`/`real_surplus`
- تفکیک کلاس (Split): تصادفی یا دستی، خودکار کلاس هم‌سطح خالی پیدا/می‌سازه
- الگوریتم تخصیص خودکار: اولویت با کلاس‌های بزرگ‌تر و کم‌شماره‌تر
- سوابق مالی دانش‌آموز + گزارش دقیق ثبت‌نام؛ اصلاح `print-table.js` برای A4 افقی؛
  سرچ اساتید و کتابخانه

### دور ۵۷ تا ۶۰ — ثبت‌نام مستقیم دانش‌آموز (وب+اپ) + پیشنهاد هوشمند سطح
- ویژگی بزرگ «ثبت‌نام مستقیم دانش‌آموز»: هم پنل وب هم اپ («ثبت‌نام دوره ترمیک»)
- تابع مرکزی `_compute_level_suggestion`: اعتبار سطح ۶۰ روزه + سطح پایانی گروه
  سنی (i5/teen15) + `LevelRenewalApproval` (تایید مدیر آموزش به‌جای تعیین‌سطح مجدد)
- ثبت‌نام کارت‌به‌کارتِ خودِ دانش‌آموز از اپ (`SelfEnrollView`) تا تایید مدیر
  «در انتظار»ست؛ مودال مرکزی خودکار در مدیریت کلاس‌ها
- یکتایی کد ملی بین کلاس‌ها؛ سوابق آموزشی هر دانش‌آموز؛ سطوح سفارشی در شهریه
- فلوتینگ‌شدن نوارهای بالای صفحات؛ مخفی‌سازی پیش‌فرض آمار/تخصیص؛ فیلد تاریخ جلسه

### دور ۶۱ — سوابق آموزشی کامل (کلاس خصوصی/گروهی/ورکشاپ)
- `StudentEducationHistoryView` تکمیل شد: علاوه‌بر ترمیک، سوابق `ClassRequest`
  (اپ `accounts`/`classes`) و `GroupSessionParticipant` (اپ `group_classes`) هم
  اضافه شد — سطح فعلی، نوع کلاس، استاد، وضعیت، جلسات، مبلغ، تاریخ‌های شمسی

### دور ۶۲ — رفع دو باگ تطبیق سطح
- چک تطبیق سطح موقع ثبت‌نام دستی از فیلد اشتباه (`student.language_level`، همیشه
  خالی) به سطح واقعیِ محاسبه‌شده اصلاح شد؛ با پیام هشدار + `force_level_mismatch`
- تطبیق سطح کلاس‌ها از exact-match به نرمال‌شده (`_classes_matching_level`) تغییر
  کرد — رفع مشکل «کلاس هم‌سطح پیدا نشد» به‌خاطر فاصله/بزرگ‌کوچیکی حروف

### دور ۶۳ — چهار اصلاح کوچک
- تاریخ جلسه از خودِ صفحه حذف و به شکل جای خالی زیر ستون هر جلسه در
  خروجی اکسل/چاپ منتقل شد؛ سطح `tns` (استارتر نوجوان) اضافه شد؛ نام استاد در
  فرم‌های تعریف کلاس از تایپی به کرکره تبدیل شد؛ پیکر تاریخ اتمام کلاس در
  `group-classes.tsx` (اپ) از میلادی به `JalaliDateTimePicker` مشترک اصلاح شد

### دور ۶۴ — سطوح استاندارد به‌عنوان منبع واحد و مدیریت‌شونده
- **رفع ریشه‌ای:** سطوح دیگه فایل هاردکد پایتونی (`level_tests/levels.py`) نبودن؛
  مدل جدید `StandardLevel` (در اپ `level_tests`) ساخته شد که تنها مرجع سطوحه —
  پنل «🎚 تعریف سطوح استاندارد» برای افزودن/حذف؛ `LevelChoicesView` و
  `infer_age_group_from_level` (در `class_management`) هر دو از همین جدول
  می‌خونن؛ همه‌ی فیلدهای تایپیِ سطح در وب (تعریف کلاس، ثبت‌نام، فیلتر گزارش،
  تخصیص) به کرکره تبدیل شدن؛ خودکار seed از لیست قدیمی + سطح tns هنگام اولین اجرا

### دور ۶۵ — کتابخانه: افزایش موجودی + هزینه/درآمد/سود
- دکمه‌ی «+ افزودن به موجودی» با مدل `BookStockAddition` (تاریخچه‌ی افزایش‌ها)
- فیلد `purchase_price` (قیمت خرید) جدا از `unit_price` (قیمت فروش)؛ محاسبه‌ی
  `total_cost`/`cost_of_goods_sold`/`total_profit` به‌تفکیک هر کتاب و در کل
- **رفع باگ منطقی:** `predicted_need` (نیاز پیش‌بینی‌شده) قبلاً با فروش زیاد
  می‌شد (چون بر مبنای کم‌شدن موجودی حساب می‌شد)؛ الان بر مبنای
  `predicted_students - total_sales_quantity` حساب می‌شه — با هر فروش کم می‌شه

---

## ۳. وضعیت فعلی هر بخش (خلاصه‌ی نهایی)

### `class_management` (بزرگ‌ترین و کامل‌ترین ماژول)
مدل‌ها: `ClassSlot`, `ClassSlotEnrollment` (+`receipt_image`/`self_enrolled`/
`payment_verified`), `TuitionSetting`, `DiscountedPerson`, `EnrollmentRefund`,
`WalletTransaction`, `LevelRenewalApproval`.
ویژگی‌های کامل: ساخت دسته‌جمعی کلاس فیزیکی، تخصیص خودکار (اولویت کلاس بزرگ/
کم‌شماره)، ثبت‌نام دستی و مستقیم و خودکار(اپ)، استرداد/انتقال/کیف‌پول/تفکیک،
تعریف شهریه (وابسته به `StandardLevel`)، گزارش دقیق، سوابق مالی/آموزشی، پنجره‌ی
تایید ثبت‌نام‌های در انتظار، تایید تمدید سطح.

### `level_tests`
مدل‌ها: `LevelTest` (+ فیلد `gender`)، `LevelTestPriceSetting`، `StandardLevel`
(منبع واحد سطوح استاندارد، دور ۶۴). وقتی تعیین‌سطح تکمیل می‌شه، خودکار به حساب
دانش‌آموزیِ موجود (بر اساس کد ملی) وصل یا حساب تازه ساخته می‌شه.

### `library`
مدل‌ها: `Book` (+ `purchase_price`)، `BookSale`، `BookStockAddition` (دور ۶۵).
موجودی، فروش، افزایش موجودی، هزینه/درآمد/سود، پیش‌بینی نیاز، سرچ فلوتینگ.

### اپ موبایل
صفحات: `home.tsx` (گرید آیکونی + دوره‌های ترمیک من)، `direct-enrollment.tsx`
(ثبت‌نام دوره ترمیک با پیکر شمسی و هشدارهای اعتبار سطح)، `group-classes.tsx`
(پیکر تاریخ اتمام حالا شمسی)، `request-form.tsx` (الگوی آپلود رسید، مبنای
`appendReceipt` که در `direct-enrollment.tsx` هم استفاده شد).

---

## ۴. کارهای ناتمومِ فوری (اولویت بالا)

- [ ] **migration دور ۶۴/۶۵ هنوز روی سیستم کاربر اجرا نشده باشه، احتمالاً**:
  ```
  python manage.py makemigrations class_management level_tests library
  python manage.py migrate
  ```
- [ ] **صفحه‌ی «افراد دارای تخفیف» (`Discounts.js`) هنوز گرفته نشده** — بک‌اندش
  (`GET /api/class-management/discounted-persons/`) از دور ۵۴ کار می‌کنه
- [ ] **درگاه پرداخت واقعی** — فقط UI و پیام «به‌زودی»؛ کاربر گفته بعداً برای
  «ثبت کلاس خصوصی توسط دانش‌آموز» هم لازمه — نیاز به تصمیم provider + merchant ID
- [ ] **فلوتینگ‌کردن هدر بقیه‌ی صفحات پنل ادمین** — فقط صفحاتی که فایلشون گرفته
  شده (`ClassManagement`, `Students`, `TeacherManagement`, `Library`) این تغییر
  رو گرفتن؛ کاربر گفته «همه‌ی صفحات پنل» رو می‌خواد ولی هنوز فایل‌هاشون نرسیده
- [ ] **صفحه‌ی مجزای پروفایل مدیر آموزش** — اگه وجود داره و کاربر می‌خواد پنل
  «تایید تمدید سطح» به‌جای `ClassManagement.js` اونجا باشه، فایلش لازمه
- [ ] **بررسی علت پاک‌شدن یوزر/رمز ادمین با هر بار اجرای `runserver`** — فرضیه‌ی
  اصلی: `DATABASES` در `core/settings.py` احتمالاً `:memory:` است (کاربر هنوز نفرستاده)
- [ ] بررسی اینکه آیا `PushToken` واقعاً ثبت می‌شه و پوش واقعاً به گوشی می‌رسه
- [ ] تصمیم و اجرای اسم جدید دلخواه برای پنل ادمین و اپ
- [ ] لوگوی دلخواه (آیکون اصلی اپ + نسخه‌ی تک‌رنگ نوار اعلان)

## ۵. کارهای خواسته‌شده ولی هنوز شروع‌نشده

- [ ] لوگو توی صفحه‌ی ورود اپ موبایل
- [ ] نمایش نسخه‌ی اپ + نام منتشرکننده داخل خودِ اپ
- [ ] Deploy واقعی بک‌اند روی VPS پارس‌پک (نصب پایتون/جنگو/دیتابیس/Gunicorn/Nginx)
- [ ] خرید دامنه، اتصال DNS، SSL (Let's Encrypt)
- [ ] تغییر آدرس API اپ از IP لوکال به دامنه‌ی واقعی https + build نهایی
- [ ] پاک‌سازی پوشه‌های تکراری/اضافی توی ریشه‌ی ریپوی موبایل

---

## ۶. نکات فنی حیاتی (برای جلوگیری از تکرار اشتباهات قبلی)

### محدودیت شبکه (بسیار مهم، هر بار تکرار شده)
- کامپیوتر توسعه‌دهنده **بدون VPN** به `api.expo.dev`, `storage.googleapis.com`,
  `developer.android.com`, `exp.host` دسترسی نداره؛ VPN هم فعلاً کار نمی‌کنه
- برای build همیشه از **«Build from GitHub»** در داشبورد وب expo.dev، نه
  `eas build`/`eas update` محلی
- برای دیدن خطای واقعی build، مستقیم بخش Logs رو با × قرمز دنبال کن، نه
  «Copy error as prompt»

### شناسه‌های فنی مهم پروژه
- Package name اندروید: `com.languageschool.app`
- نام اپ (نمایشی): My Language School
- EAS project ID: `1ba99ba7-db51-4728-b7c3-cad26e1af0f9`
- VPS: پارس‌پک، پلن irVPS3 (۲گیگ رم، ۲هسته، ۵۰گیگ SSD، Ubuntu) — هنوز دیپلوی نشده

### الگوهای رایج خطای build اکسپو
- فایل آیکون تعریف‌شده در `app.json` که واقعاً وجود نداره
- `projectId` جایگزین‌نشده در `app.json`
- ناهماهنگی نسخه‌ی پکیج‌ها بعد از نصب جدید → `npx expo install --fix`
- استفاده از پکیج جدید بدون نصب صریح → همیشه `npx expo install <package>`

### درس‌های مهم این چند دور اخیر (برای این چت و چت‌های بعدی)
- **بعد از هر ویرایش دستی روی `views.py` (یا هر فایل بزرگ)**، حتماً
  `grep -n "^class "` بزن و مطمئن شو هیچ کلاسی سهواً ادغام/حذف نشده (یه بار دقیقاً
  همین اتفاق افتاد و کرش کامل داد)
- سطوح (levels) هرگز نباید تایپی باشن — همه‌جا باید از `StandardLevel`/
  `levelChoices` بیان، وگرنه مشکلات تطبیق (مثل «کلاس هم‌سطح پیدا نشد») برمی‌گرده
- هر پراپرتی محاسبه‌ای که «کم‌شدن با یه رویداد» رو نشون می‌ده (مثل `predicted_need`)
  باید دقیق فکر بشه رویداد داره از کدوم عدد کم می‌کنه — منطق اشتباه راحت جا میفته

---

## ۷. فایل‌های یادآور مرتبط (برای مطالعه‌ی تکمیلی و جزئیات فنیِ دقیق‌تر)
- **همین فایل** — خلاصه‌ی کامل و به‌روز از صفر تا انتها (اولین مرجع)
- `ROUND-57-60-DIRECT-ENROLLMENT-WORKFLOW.md` — جزئیات فنی کامل ثبت‌نام مستقیم،
  پیشنهاد سطح، تایید تمدید، ثبت‌نام‌های در انتظار
- `ROUND-55-56-CLASS-MANAGEMENT-BUGFIXES.md` — جزئیات باگ ریشه‌ای تخصیص/ثبت‌نام،
  تفکیک کلاس
- `ROUND-54-CLASS-MANAGEMENT-UPGRADE.md` — جزئیات شهریه/تخفیف/استرداد/کیف‌پول
- `PROJECT-FULL-CONTEXT.md` — جزئیات کامل فنی مدل‌ها/API/کد (به‌روز تا دور ۵۲،
  قدیمی‌ترین سند، برای تاریخچه‌ی خیلی اولیه)
- `PUSH-NOTIFICATIONS-SETUP-PROGRESS.md` — جزئیات مراحل راه‌اندازی پوش
- `DEPLOY-SETUP-PROGRESS.md` — جزئیات خرید/راه‌اندازی VPS و دامنه
- `NETWORK-ERROR-REPORT.md` — گزارش تاریخیِ عیب‌یابی مشکل شبکه (حل‌شده)
- `PROJECT-STATUS-AFTER-NETWORK-FIX.md` — وضعیت بلافاصله بعد از حل مشکل شبکه

**نکته‌ی مهم درباره‌ی دورهای ۶۱ تا ۶۵:** فایل مرحله‌ای جدا براشون ساخته نشده —
همه‌ی جزئیاتشون همینجا (بخش ۲) خلاصه شده. اگه جزئیات فنیِ خیلی دقیق (نام دقیق
هر endpoint/فیلد) لازم شد، باید از خودِ کد فعلی (که توی گیت‌هاب پوش شده) خوند.
